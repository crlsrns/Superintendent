<% if @kases.empty? %>
  <p>
    There are no closed cases.
  </p>
<% else %>
  <h1>Closed Cases</h1>
  <ol class="report">
    <% @kases.each do |kase| %>
    <li><%= link_to kase.name, url_to_kase(kase.id) %></li>
    <% end %>
  </ol>
<% end %>
