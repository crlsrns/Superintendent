<% if @kases.empty? %>
  <p>
    There are no open cases.
  </p>
<% else %>
  <h1>Open Cases</h1>
  <ol class="report">
    <% @kases.each do |kase| %>
      <li><%= link_to kase.name, url_to_kase(kase.id) %></li>
    <% end %>
  </ol>
<% end %>
